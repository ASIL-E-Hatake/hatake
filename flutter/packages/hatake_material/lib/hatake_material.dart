/// hatake_material — a Material 3 renderer for hatake.
///
/// Re-exports hatake so an app can import a single package to get both the
/// runtime and this renderer.
library;

export 'package:hatake/hatake.dart';

export 'src/material_renderer.dart';
export 'src/material_theme.dart';
