# 変更の記録

版ごとに「何が変わったか」を残す。**3版（Flutter / TypeScript / Java）は同じ番号で出す**
ので、この1枚で3版ぶんを書く（→ [1.0 の約束](docs/compat.ja.md#版の足並み)）。

並びは新しいものが上。見出しは `## <版> — <日付>`。

各項目の頭に印を付ける:

| 印 | 意味 |
|---|---|
| **破壊** | 相手のものが動かなくなる変更。**major を上げる** |
| 追加 | 足しただけ。上げるのは minor |
| 修正 | 直しただけ。上げるのは patch |
| 内部 | 使う側から見て何も変わらない（書かなくてもよいが、大きいものは残す） |

DSL の版（`dsl_version`）はパッケージの版とは別に動く。DSL が上がった版にはその旨を書く。

## 0.9.2 — 2026-09-18

- 修正: **数字とグラフの画面と帳票でも、一覧のコードを名前で出す**ようにした。
  0.9.1 で crud / search は直したが、**ダッシュボードのカードと帳票の明細は
  そのまま**で、`draft` / `confirmed` が並んでいた（見本2本目のダッシュボードの
  スクリーンショットに残って気づいた）。紙に刷る側（`hatake_print`）も同じにした＝
  **画面の字と紙の字を変えない**（`confirmed` のまま刷った紙は取引先に送れない）。
  引く相手はその画面の**出力条件に書いてある選択肢**だけ（入力を持たない画面なので）。
  相変わらず列に `options` は書かせない＝同じことを2か所に書かせない

## 0.9.1 — 2026-09-17

**外から使って出た穴を埋めた版。** 見本（hatake-example）で業務アプリを2本作る途中で
出たものばかりで、**どれも枠組みの中のテストでは出なかった**。

- 追加: **形の違う値を書いたキー**を言うようにした（`key-wrong-shape`）。綴りが合って
  いるキーに形の違う値を書くと、解析器は「読める形か」を見て**黙って捨てる**＝書いたのに
  一度も効かない。見本を作っていて `optionsFrom:` の下に Repository を書き
  （正しくは `optionsSource`）、**検証は exit 0・画面も出て・選択肢だけが空**という所を
  踏んだ。しかも生成した設計書まで「選択」としか書かれず、静かに嘘をついていた。
  目で読んでもキー名は正しいので、いちばん気づきにくい。見るのは**形が一意に決まる
  キーだけ**（`filters` はカードでは入れ子・検索では並びなので見ない）
- 追加: **Java 版で app 定義の画面を中身まで読めるようにした**
  （`AppParser.parseAppPagesYaml`）。いままで Java から取れたのは `PageRef`
  （id・種別・タイトル・Repository）だけで、**`form` も `search` も取れなかった**＝
  「画面と同じ定義でサーバでも検証する」というこの枠組みの主張が、Java 版だけ通らな
  かった（Dart 版と TypeScript 版には在った）。2本目の見本で API を Java にして初めて
  出た。`dsl_version` と strict の門番は app として1回通してから画面を読む
  （隣の画面が壊れている app に気づけるように）
- 追加: **Java 版が列の `roles` を読むようにした**（`ColumnDefinition.roles`）。
  「描画専用のキーは持たない」として落としていたが、列の `roles` が決めるのは
  **誰がそのデータを見てよいか**で、画面で隠すだけなら API を直接叩けば取れる＝
  サーバでも同じ定義から同じ列を落とせないと、隠したことにならない
  （`hatake ask` の `authz-server` がまさにこれを聞く）。判定は `Access`（3版で同じ）
- 修正: **正しい言葉で書いてあるラベルを、用語辞書が叱っていた**
  （`project-glossary-word`）。`avoid` に並べた言葉が正しい言葉の一部になっていると
  （「数量」の `avoid` に「数」）、**正しいラベルが自分自身で叱られて言い直しようが
  なかった**。その用語の分だけ見る＝他の用語の「呼ばない言葉」は今までどおり出る
- 修正: **JitPack が Java 版をビルドできなかった**（`./gradlew: Permission denied`）。
  `java/gradlew` に実行ビットが入っていなかった。`jitpack.yml` も `sh gradlew` に
  変えて二重に守る（Windows で clone したときに落ちないように）。
  **v0.9.0 の JitPack は落ちたままです**（tag の中身は動かせないので）。この版から直ります
- 追加: **見るものが書かれていない検証**を言うようにした（`validator-missing-param`）。
  検証は種類ごとに見るキーが違う（`maxLength` は `value`、`pattern` は `pattern`）のに、
  名前を間違えても**解析もスキーマも通って、その検証は一度も効かなかった**。
  見本を作っていて `{ type: pattern, value: "^[0-9]{6}$" }` が警告ゼロで通り、
  6桁の規則が素通りしているのに気づいた。組み込みだけを見る（独自の検証は開いた
  文字列なので、何を見るか枠組みは知らない）
- 修正: **一覧のコードを名前で出す**ようにした。入力欄では `options` のラベルが出るのに、
  一覧では `active` / `corp` と生のコードが出ていた（同じ画面で同じ項目が2つの顔を持つ）。
  **列に `options` を書けるようにはしない**＝同じことを2か所に書かせると、片方だけ
  直したときに一覧と入力で違う字が出る。同じ画面の入力欄と検索条件から引く
  （`optionLabelOf` / `optionLabelIn`）

## 0.9.0 — 2026-09-16

**最初に配る版**（git の tag のみ。レジストリには出していない）。
凍らせた約束は [1.0 の約束](docs/compat.ja.md)。

- 修正: `buildQuery` / `QueryBuilder` が **`sortAscending` の文字列 `"false"` を
  昇順として読んでいた**（TypeScript / Java）。REST の契約
  （`spec/conformance/rest_query.json`）は**クエリ文字列で送る**と決めているので、
  手引きどおり `buildQuery(page.search, req.query)` と書いたサーバは、
  **降順を頼まれているのに黙って昇順で返していた**（画面には並びが出るので気づけない）。
  契約の両端が食い違っていた形。共有フィクスチャ（`spec/conformance/queries.json`）に
  文字列の `"false"` / `"true"` の場合を足して、3版で固定した

- 追加: **git の tag だけで配れるようにした**（→ [リリースと入れ方](docs/guide/release.ja.md)）。
  レジストリには出さない。リリースのワークフローは GitHub Release に貼るだけで、
  **publish する道を持たない**
  - TypeScript … `npm pack` に `spec/` と LICENSE を同梱（CLI と MCP は実行時に `spec/`
    を読むので、同梱しないと配った先で何も引けない）。`.tgz` を Release に貼る。
    **固めてリポジトリの外に入れて叩く**ところまで CI が見る（中で試すと `spec/` が
    上に見つかって、同梱できていなくても通る）
  - Flutter / Dart … git 依存が本当に解決できることを CI が確かめる（裸のクローンを
    `file://` で指して `pub get` → 定義を1枚読ませる）。**`dependency_overrides` を
    書かないと落ちること**も確かめる＝手引きが古くなったらそこで落ちる
  - Java … `jitpack.yml` と `maven-publish`（JitPack が tag からビルドする）。
    Gradle の wrapper も入れた
  - 手引きの git 依存に **`ref:`（tag）** を書くようにした。`main` を指すと、こちらが
    push した瞬間に相手が動く

- 追加: **DSL の版を見るようになった。** 知らない major（`2.0` / `0.9`）と、形が違う字
  （`1` / `1.0.0` / 空）は**落とす**。同じ major の新しい minor（`1.1`）は読んで1件言う
  （`dsl-version-newer`）。判定は3版＋JSON Schema で同じ
  （`spec/conformance/dsl_version.json`）。
  → これまでは**何を書いても黙って `1.0` として読んでいた**ので、今まで通っていた定義が
  落ちることがある（落ちるのは、そもそも読めていなかったもの）
- 追加: 公開した診断の id を凍らせた（`spec/rule-ids.json`・警告 94 / 助言 21）。
  試験が完全一致を見るので、**消す・名前を変える差分はレビューで必ず目に入る**
- 追加: TypeScript の公開 API の一覧（`spec/public-api.ts.json`）。
  `index` から出ているものが変わったら試験が落ちる
- 修正: page のスキーマの `$id` が引けない URL（`/raw/main/` の無い形）だった。
  他の2枚と同じ形に揃え、試験で見るようにした
- 追加: [1.0 の約束](docs/compat.ja.md)。凍らせるもの・凍らせないもの・終了コード・
  `--json` の形・版の足並み・名前の決めごとを1枚に
- **破壊**: npm のパッケージ名を `@hatake/core` → **`@hatake-fw/api`** に変えた
  （まだ publish していないので、実害があるのは git で入れていた人だけ）。
  無印 `hatake` も `@hatake` スコープも他人のものだったため。`core` と名乗らないのは、
  Renderer（`vue` / `react`）を足す段で `core` を切り出せるようにするため
  ＝**いまの中身はサーバ側のロジック＋道具**で、描画は1行も無い

## 0.0.1

最初の形（まだどこにも配っていない）。ここまでの中身は
[ロードマップ](docs/roadmap.ja.md)の「A. 機能として用意すべきもの」を見る。
