# 変更の記録

版ごとに「何が変わったか」を残す。**3版（Flutter / TypeScript / Java）は同じ番号で出す**
ので、この1枚で3版ぶんを書く（→ [1.0 の約束](docs/compat.ja.md#版の足並み)）。

並びは新しいものが上。見出しは `## <版> — <日付>`。

各項目の頭に印を付ける:

| 印 | 意味 |
|---|---|
| **破壊** | 相手のものが動かなくなる変更。**major を上げる** |
| 追加 | 足しただけ。上げるのは minor |
| 修正 | 直しただけ。上げるのは patch |
| 内部 | 使う側から見て何も変わらない（書かなくてもよいが、大きいものは残す） |

DSL の版（`dsl_version`）はパッケージの版とは別に動く。DSL が上がった版にはその旨を書く。

## 0.9.9 — 2026-09-24

**DSL のキーを、全部いちど書いて動かした。** 台帳（`tool/check-coverage.mjs`）が
数えている **(ノード, キー) 255 組のうち 40 組が、どの例にも一度も書かれていなかった**
＝動かしたことがなかった。1.0 で凍らせる前に全部書く、と決めていた宿題を片付けた。
**255/255（除外ゼロ）**になった。

書いて動かしたら、案の定1件出た。

- 修正: **畳んだ残りの言い方（`overflow`）に、書けない差し込みを書いても黙って
  文字で出ていた。** ここに書けるのは `{count}`（**隠れた行数**）だけなのに、
  `{rest}` と書いても検証は何も言わず、画面に `{rest}` とそのまま出た。
  原因は差し込みの台帳（`spec/placeholders.json`）に**この文脈が載っていなかった**
  こと ── 規則は台帳から作っているので、載っていない所は一度も見ていなかった。
  「**閉じた集合**」と謳っている台帳に穴が空いていた
- 追加: 差し込みの台帳に **4つ目の文脈**（`computed-overflow`）。ここだけ
  `{count}` の意味が違う（ボタンの文言では「件数」、ここでは「出さなかった行数」）
- 内部: 同梱の例に、これまで一度も書かれていなかったキーを全部書いた。
  `optionsSource`（Repository から選択肢を引く。**どの例にも無かった**）・
  条件の組み合わせ（`all` / `any` / `not`）・押す前に聞く一括（`prompt` / `batchSize`）・
  畳む行の並べ替え（`sort` / `limit` / `overflow`）・テーマの全キー・
  `navigation` と `open: tab` の対・`defaultValue`・`validator.message` ほか
- 内部: 影響分析の試験が**件数**で見ていたのを**道の一覧**に変えた。件数だと、例に
  1つ増えたときに「4 を期待して 3 でした」としか出ず、何が増えたのか読めない
- 内部: **生成物の作り直しを1本にまとめた**（`tool/regen.mjs`）。作り直す手順が
  **CI にしか書いていなかった**＝どれが生成物なのかは落ちて初めて分かる。実際、
  見本の画面にボタンを1つ足しただけで遷移図が古くなって落ち、それを直した直後に
  今度は**サイトに貼ってある図**が古くて落ちた（2回続けて同じ種類で落ちた）。
  **手元と CI で同じ道具を回す**ようにして、古い生成物はファイル名を名指しで言う。
  見る相手は3つ ── `docs/diagrams/*.svg`・`spec/reference.json`・
  **`site/docs/diagrams.md` に貼ってある囲み**。3つ目はページ自体が手書きなので、
  ファイルの一覧を見ても生成物だと分からない

## 0.9.8 — 2026-09-24

**画面が叩く口を、宣言にも出す。** 詳細画面（`type: detail`）は `findByKey` を必ず
呼ぶのに、`hatake openapi` はその画面から**道を1本も出していなかった**。サーバを
書く人は、詳細画面のぶんだけ手で足すことになっていた。複合キーを入れたときに気づいた
（鍵の形とは無関係で、前から在った）。

- 修正: **`detail` 画面が `GET <collection>/<鍵>` を宣言する**。返す形（`response`）を
  その画面の `form` から作る。**書く形は作らない**＝読むだけの画面に `POST` や `PUT` を
  生やすと、定義に書いていない口を宣言することになる
- 修正: **一覧を持たない画面の一覧を `probe` が叩かなくなった**。定義が要求していない
  口なので、サーバが持っていなければ 404 が返り、**在りもしない食い違い**
  （「その口がありません」）を報告していた。叩かなかったことは黙らずに言う
- 内部: `--dry-run`（送る前に見せる）と実際に送るものを同じ判断にした。
  ここがズレると「見せてから違うものを送る」道具になる

## 0.9.7 — 2026-09-24

**複合キー。** 1件が2つ以上の列で決まる画面（受注明細・履歴表・年度別の実績）を
持てるようにした。**1.0 のあとには入れられない**変更なので、凍らせる前に入れる。

```yaml
key: [orderNo, lineNo]
```

- 追加: **`key` が並びを取る**。書いた順がそのまま REST の道の順になる
  （`/api/orderLines/SO-1/2`）。並べ替えると別の1件を指すので、定義の並びは
  URL の一部だと思って扱う
- 追加: **`RecordKey`**（3版）＝項目名と値の組。**値で比べられる**のが要点で、
  鍵は画面の中で「選んだ行の集まり」として持ち回っているため、素の地図だと
  同じ行を2回選べて一括が数え違える
- 追加: 検証 **`key-duplicate-field`**＝複合キーに同じ項目が2回。鍵は項目名で
  束ねるので1つ分にしかならないのに、出す API の道は書いた数だけ区切りを宣言する
  ＝**宣言と実際がずれる**
- 追加: 検証 **`key-single-in-list`** / **`key-empty`**
- 追加: 見本 `spec/examples/order_lines.yaml`
- **破壊**: 3版の `keyField`（文字）を **`keyFields`（並び）**にした。
  `PageDefinition` / `SubTableSource` / `RestTarget` と、Dart の
  `page.recordKeyField` → `recordKeyFields`、`FakeRepository` の引数が変わる
- **破壊ではない**: **`Repository` の口は変えていない**（`findByKey(Object)` は
  もともと `Object`）。1件を指す項目が1つの画面には**いままでどおり素の値**が渡るので、
  既に動いている Repository は**1行も直さなくてよい**。複合キーを使うと決めた画面
  だけが `RecordKey` を見る
- 内部: `hatake openapi` / `probe` / `wire` が同じ並びで道を組む。3版とも
  `spec/conformance/dto_spec.json` の複合キーの事例で縛る
- 内部: **画面の枚数の突き合わせ**（`tool/check-screen-count.mjs`）。枚数は
  「3版で同じ答えが出る」ことの確かめに使っていて**4か所に直に書いて**あるが、
  例を1枚足したときに2か所を取りこぼして CI で落ちた。**どこを直せばいいかは
  落ちたログに出てこない**（3か所目・4か所目は直して回し直してから分かる）ので、
  数え直して食い違いを一度に名指しするようにした

## 0.9.6 — 2026-09-24

**「緑なのに何も確かめていない」を潰した版。** 前の版の検証中に、自分で書いた定義と
シナリオが**3つとも黙って素通りした**。どれも「書いたのに効かない」で、しかも結果は
エラーでも空欄でもなく**0 と緑**という、いちばん気づけない形だった。

- 追加: **シナリオの知らないキーで止まる**（`hatake run --scenario`）。値の入り口は
  `record` なので `input` と書くと黙って捨てられ、**空のレコードで動いて
  「1 件すべて期待どおり」**になっていた。動かして確かめる道具が、何も入れずに
  緑を返すのがいちばん困る。よくある書き間違い（`input` / `values` / `then` …）は
  **正しい名前を添える**。1件目で止めず全部並べる
- 追加: 検証 **`computed-without-source`**＝`op: sum` に `fields` も `field` も無い。
  **空欄ではなく 0** になるので、請求書の金額が黙って 0 円で出る。`sum` のように
  両方のモードに在る op が、畳む相手なしで素通りしていた
- 追加: **`of` に並びを書いたら言う**（`key-wrong-shape`）。`of` は明細の行の
  どの値を見るかを**文字ひとつ**で書く所。同じレコードの項目を足すのは `fields`

## 0.9.5 — 2026-09-24

**同じことを2回書かせない版。** 見本2本を数えたら、同じコード表（`shipped` → 出荷済）を
3回・2回・3回と書いていた。片方だけ直すと画面ごとに違う字が出て、しかも直すまで誰も
気づかない。あわせて、`openapi` が**画面1枚しか読めなかった**のも直した（業務の定義は
app なので、結局サーバの API 一覧を手で書くことになっていた）。

- 追加: **`app.vocabularies`**＝アプリ全体のコード表。名前と選択肢の並びを1か所に書く
- 追加: **`optionsOf: <名前>`**＝その語彙を指す。書けるのは **`field` / `filter` /
  `column`** の3か所。**列にも書ける**のが肝で、これが無かったから 0.9.2 は
  「同じ画面から借りる」裏技を足すしかなく、検索欄の無い画面（帳票・詳細）は
  コードのまま出ていた。列に `options` を直接書けるようにはしない
  （見せる所に並びをもう一度書かせると、入力と片方だけ直したときに食い違うので）
- 追加: 検証 **`unknown-vocabulary`**＝指した語彙が無い（選択肢が空のまま画面が出る）。
  近い名前があれば「もしかして」を添える
- 追加: 検証 **`vocabulary-shadowed`**＝同じ所に `options` と `optionsOf` の両方。
  その場の並びが勝つので `optionsOf` は一度も効かない
- 追加: 検証 **`duplicate-vocabulary`**＝同じ名前の語彙が2つ。先に書いたほうが使われる
- 追加: **`hatake openapi` が app を読む**。全画面ぶんを1枚にまとめる。
  `--base-path` は**基点だけ**（`/api`）で、区切りは Repository 名から推測する
  （`wire` / `probe` と同じ推測）。同じ Repository を見る画面は**1つの資源**に
  まとまり、口がぶつかったものと受け口を持たない画面は**黙らずに**標準エラーへ出す
- 追加: **`hatake schema` が app を読む**（画面 id をキーにした1枚）
- **破壊**: 3版の `ColumnDefinition` に `options` を足した。定義には書けず、
  読み込み時に語彙から入る（Java は record の構成要素が増える）
- 内部: 語彙の展開は**読み込み時**。`optionsOf` を実体の並びに置き換えてから解析器に
  渡すので、Renderer も CSV も紙も**この仕組みを知らないまま**正しく動く。
  3版で同じ判断をすることは `spec/conformance/vocabularies.json` が縛る

## 0.9.4 — 2026-09-24

**「値を文字にする」を1か所にまとめた版。** 0.9.3 で直した詳細画面の不具合を追って
いったら、同じ4行が**7か所に写し取られて**いて、0.9.2 で「コードではなく名前を出す」を
足したときに直せたのは**そのうち3か所だけ**だと分かった。残りは画面によって同じ値が
別の顔で出たままだった。**写し取りは単体試験では見つからない**（どの写しも自分の中では
正しい）ので、写し取りをやめる。

- **破壊**: `toCsv` に `owners`（その画面の選択肢）を足した。3版とも**引数が1つ増える**。
  既定は空なので、渡さなければ今までと同じ字で落ちる
- **破壊**: Java の `FieldDefinition` / `FilterDefinition` に `options` を足した。
  record の構成要素が増えるので、正式コンストラクタを直に呼んでいる所は直しが要る
  （短縮コンストラクタは今までどおり）
- 修正: **詳細画面の明細が、入れ子のまま1行で出ていた。**
  `[{orderNo: SO2026070001, lineNo: 1, …}]` という字が納品用のスクリーンショットに
  残っていた。これからは**表として**出る（`type: subTable` の `columns` の並びで）
- 修正: **詳細画面が選択肢の名前を引いていなかった。** 一覧では「出荷済」と出る値が、
  同じ画面の詳細では `shipped` のままだった
- 修正: **明細の中の選択肢を引いていなかった。** 明細は `rowFields` の選択肢で引く
  （親の画面の選択肢とは別物なので混ぜない）
- 修正: **CSV が画面と違う字で落ちていた。** 一覧に「出荷済」と出ている列が
  `shipped` で落ちていた。`raw: true` は今までどおり素の値（取り込み直す相手が居る）
- 修正: **選択肢の名前を出す仕掛けが Dart にしか入っていなかった。** 0.9.2 の修正が
  TypeScript と Java に入っておらず、CSV は「3版で同じ文字列」の約束を静かに破って
  いた。3版そろえ、`spec/conformance/csv.json` に**選択肢つきの事例を5件**足して縛った
- 追加: Java に `OptionItem` と `CellText`。サーバ側も**同じ語彙を知っていないと
  同じ字にできない**（Java 版には `options` を表す型が無かった）
- 内部: `Displayed`（値を文字にするものの口）を足し、列と入力項目の両方に実装させた。
  `cellText` が1本で済み、**直す所が1か所**になる。写し取りは7か所 → 1か所

## 0.9.3 — 2026-09-19

**見本の「機能網羅アプリ」を作って出た版。** DSL のキーを一度ずつ全部書いて動かす
アプリを作ったところ、**どの例にも書かれていなかった所**から2件出た。

- 修正: **1件の画面が、`id` という名前の引数でしか鍵を受け取らなかった。**
  `key: itemCode` と書いてある画面へ `params: { itemCode: $row.itemCode }` で渡しても、
  URL は `#/…?itemCode=…` と正しく変わるのに**開いた画面は空**で、**データを取りに
  行きもしない**（エラーも出ない）。これからは**画面に書いてある `key` の名前**で
  受け取る。`id` も今までどおり効く（`key` を省いた画面の既定なので）
- 追加: 助言 **`navigate-without-key-param`**＝1件の画面へ行くのに、行き先が待って
  いる名前で鍵を渡していない。入力画面へ**何も**渡さないのは新規作成なので言わない
  （渡しているのに名前が違うときだけ言う）
- 追加: 助言 **`detail-page-in-menu`**＝詳細画面をメニューに直接置いている。
  メニューには鍵を渡す場所が無いので、開いても必ず「データがありません」になる
- 追加: **`key` に並びを書いたら言う**（`key-wrong-shape`）。`key: [orderNo, lineNo]` は
  **検証 exit 0 で素通り**していた。複合キーはいまは持っていないので、そう言う
  （逃げ道は「連結した列をビューに1つ作る」＝レシピに書いた）
- 追加: **網羅の台帳**（`tool/check-coverage.mjs`）。`spec/reference.json` の
  **(ノード, キー) 249組**のうち、どこがまだ一度も書かれていないかを数える。
  単体試験が在ることと、通しで動くアプリで書かれたことは別 ── 外から使って出た
  不具合はどれも**3版の試験が全部緑の状態**で存在していた

## 0.9.2 — 2026-09-18

- 修正: **数字とグラフの画面と帳票でも、一覧のコードを名前で出す**ようにした。
  0.9.1 で crud / search は直したが、**ダッシュボードのカードと帳票の明細は
  そのまま**で、`draft` / `confirmed` が並んでいた（見本2本目のダッシュボードの
  スクリーンショットに残って気づいた）。紙に刷る側（`hatake_print`）も同じにした＝
  **画面の字と紙の字を変えない**（`confirmed` のまま刷った紙は取引先に送れない）。
  引く相手はその画面の**出力条件に書いてある選択肢**だけ（入力を持たない画面なので）。
  相変わらず列に `options` は書かせない＝同じことを2か所に書かせない

## 0.9.1 — 2026-09-17

**外から使って出た穴を埋めた版。** 見本（hatake-example）で業務アプリを2本作る途中で
出たものばかりで、**どれも枠組みの中のテストでは出なかった**。

- 追加: **形の違う値を書いたキー**を言うようにした（`key-wrong-shape`）。綴りが合って
  いるキーに形の違う値を書くと、解析器は「読める形か」を見て**黙って捨てる**＝書いたのに
  一度も効かない。見本を作っていて `optionsFrom:` の下に Repository を書き
  （正しくは `optionsSource`）、**検証は exit 0・画面も出て・選択肢だけが空**という所を
  踏んだ。しかも生成した設計書まで「選択」としか書かれず、静かに嘘をついていた。
  目で読んでもキー名は正しいので、いちばん気づきにくい。見るのは**形が一意に決まる
  キーだけ**（`filters` はカードでは入れ子・検索では並びなので見ない）
- 追加: **Java 版で app 定義の画面を中身まで読めるようにした**
  （`AppParser.parseAppPagesYaml`）。いままで Java から取れたのは `PageRef`
  （id・種別・タイトル・Repository）だけで、**`form` も `search` も取れなかった**＝
  「画面と同じ定義でサーバでも検証する」というこの枠組みの主張が、Java 版だけ通らな
  かった（Dart 版と TypeScript 版には在った）。2本目の見本で API を Java にして初めて
  出た。`dsl_version` と strict の門番は app として1回通してから画面を読む
  （隣の画面が壊れている app に気づけるように）
- 追加: **Java 版が列の `roles` を読むようにした**（`ColumnDefinition.roles`）。
  「描画専用のキーは持たない」として落としていたが、列の `roles` が決めるのは
  **誰がそのデータを見てよいか**で、画面で隠すだけなら API を直接叩けば取れる＝
  サーバでも同じ定義から同じ列を落とせないと、隠したことにならない
  （`hatake ask` の `authz-server` がまさにこれを聞く）。判定は `Access`（3版で同じ）
- 修正: **正しい言葉で書いてあるラベルを、用語辞書が叱っていた**
  （`project-glossary-word`）。`avoid` に並べた言葉が正しい言葉の一部になっていると
  （「数量」の `avoid` に「数」）、**正しいラベルが自分自身で叱られて言い直しようが
  なかった**。その用語の分だけ見る＝他の用語の「呼ばない言葉」は今までどおり出る
- 修正: **JitPack が Java 版をビルドできなかった**（`./gradlew: Permission denied`）。
  `java/gradlew` に実行ビットが入っていなかった。`jitpack.yml` も `sh gradlew` に
  変えて二重に守る（Windows で clone したときに落ちないように）。
  **v0.9.0 の JitPack は落ちたままです**（tag の中身は動かせないので）。この版から直ります
- 追加: **見るものが書かれていない検証**を言うようにした（`validator-missing-param`）。
  検証は種類ごとに見るキーが違う（`maxLength` は `value`、`pattern` は `pattern`）のに、
  名前を間違えても**解析もスキーマも通って、その検証は一度も効かなかった**。
  見本を作っていて `{ type: pattern, value: "^[0-9]{6}$" }` が警告ゼロで通り、
  6桁の規則が素通りしているのに気づいた。組み込みだけを見る（独自の検証は開いた
  文字列なので、何を見るか枠組みは知らない）
- 修正: **一覧のコードを名前で出す**ようにした。入力欄では `options` のラベルが出るのに、
  一覧では `active` / `corp` と生のコードが出ていた（同じ画面で同じ項目が2つの顔を持つ）。
  **列に `options` を書けるようにはしない**＝同じことを2か所に書かせると、片方だけ
  直したときに一覧と入力で違う字が出る。同じ画面の入力欄と検索条件から引く
  （`optionLabelOf` / `optionLabelIn`）

## 0.9.0 — 2026-09-16

**最初に配る版**（git の tag のみ。レジストリには出していない）。
凍らせた約束は [1.0 の約束](docs/compat.ja.md)。

- 修正: `buildQuery` / `QueryBuilder` が **`sortAscending` の文字列 `"false"` を
  昇順として読んでいた**（TypeScript / Java）。REST の契約
  （`spec/conformance/rest_query.json`）は**クエリ文字列で送る**と決めているので、
  手引きどおり `buildQuery(page.search, req.query)` と書いたサーバは、
  **降順を頼まれているのに黙って昇順で返していた**（画面には並びが出るので気づけない）。
  契約の両端が食い違っていた形。共有フィクスチャ（`spec/conformance/queries.json`）に
  文字列の `"false"` / `"true"` の場合を足して、3版で固定した

- 追加: **git の tag だけで配れるようにした**（→ [リリースと入れ方](docs/guide/release.ja.md)）。
  レジストリには出さない。リリースのワークフローは GitHub Release に貼るだけで、
  **publish する道を持たない**
  - TypeScript … `npm pack` に `spec/` と LICENSE を同梱（CLI と MCP は実行時に `spec/`
    を読むので、同梱しないと配った先で何も引けない）。`.tgz` を Release に貼る。
    **固めてリポジトリの外に入れて叩く**ところまで CI が見る（中で試すと `spec/` が
    上に見つかって、同梱できていなくても通る）
  - Flutter / Dart … git 依存が本当に解決できることを CI が確かめる（裸のクローンを
    `file://` で指して `pub get` → 定義を1枚読ませる）。**`dependency_overrides` を
    書かないと落ちること**も確かめる＝手引きが古くなったらそこで落ちる
  - Java … `jitpack.yml` と `maven-publish`（JitPack が tag からビルドする）。
    Gradle の wrapper も入れた
  - 手引きの git 依存に **`ref:`（tag）** を書くようにした。`main` を指すと、こちらが
    push した瞬間に相手が動く

- 追加: **DSL の版を見るようになった。** 知らない major（`2.0` / `0.9`）と、形が違う字
  （`1` / `1.0.0` / 空）は**落とす**。同じ major の新しい minor（`1.1`）は読んで1件言う
  （`dsl-version-newer`）。判定は3版＋JSON Schema で同じ
  （`spec/conformance/dsl_version.json`）。
  → これまでは**何を書いても黙って `1.0` として読んでいた**ので、今まで通っていた定義が
  落ちることがある（落ちるのは、そもそも読めていなかったもの）
- 追加: 公開した診断の id を凍らせた（`spec/rule-ids.json`・警告 94 / 助言 21）。
  試験が完全一致を見るので、**消す・名前を変える差分はレビューで必ず目に入る**
- 追加: TypeScript の公開 API の一覧（`spec/public-api.ts.json`）。
  `index` から出ているものが変わったら試験が落ちる
- 修正: page のスキーマの `$id` が引けない URL（`/raw/main/` の無い形）だった。
  他の2枚と同じ形に揃え、試験で見るようにした
- 追加: [1.0 の約束](docs/compat.ja.md)。凍らせるもの・凍らせないもの・終了コード・
  `--json` の形・版の足並み・名前の決めごとを1枚に
- **破壊**: npm のパッケージ名を `@hatake/core` → **`@hatake-fw/api`** に変えた
  （まだ publish していないので、実害があるのは git で入れていた人だけ）。
  無印 `hatake` も `@hatake` スコープも他人のものだったため。`core` と名乗らないのは、
  Renderer（`vue` / `react`）を足す段で `core` を切り出せるようにするため
  ＝**いまの中身はサーバ側のロジック＋道具**で、描画は1行も無い

## 0.0.1

最初の形（まだどこにも配っていない）。ここまでの中身は
[ロードマップ](docs/roadmap.ja.md)の「A. 機能として用意すべきもの」を見る。
